export default {
    entries: {
        './App.js': `
export function sum(left, right) {
    return left + right;
}

export const subtract = (left, right) => {
    return left - right;
};
`,
    },
};
